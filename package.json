{
  "name": "workspace",
  "version": "1.0.0",
  "description": "This project provides equivalent HTTP server implementations using both Flask (Python) and Express.js (Node.js) with a web-based testing interface.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
